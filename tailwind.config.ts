import type { Config } from "tailwindcss";

const config: Config = {
  theme: {
    extend: {
      colors: {
        background: "#0f172a",
        card: "#111827",
        border: "#1f2937",
        primary: "#6366f1",
      },
    },
  },
  plugins: [],
};

export default config;